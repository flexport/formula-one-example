// @flow

import * as React from "react";

// type NumberValue = {
//   raw: string,
//   parsed: Either<string, number>,
// }

// type Props = {|
//   value: [string, number],
//   onChange: (Either<string, number>) => void,
// |};

// export default class NumberInput extends React.{
//   handleChange =
// }
